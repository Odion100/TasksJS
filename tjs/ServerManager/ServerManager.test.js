const { expect } = require("chai");
const TasksJSServerManager = require("./ServerManager");
describe("ServerManager", () => {
  const ServerManager = TasksJSServerManager();

  it("should return an TasksJSServerManager instance", () => {});

  it("should accept GET request for ServerModule Connection Data on the given route", () => {});

  it("should be able to added ServerModule data to the ServerManager instance that can be accessed via a GET request", () => {});
});
